import { Typography } from '@mui/material';
import React from 'react';

const ManageOrders = () => {
    return (
        <div>
            <Typography variant="h4">Manage All Order</Typography>
        </div>
    );
};

export default ManageOrders;